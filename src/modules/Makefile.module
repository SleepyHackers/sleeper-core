ROOT := ../../..
OBJS := ${patsubst %.c,%.o,${filter %.c, ${SRC}}}
OUT := ${ROOT}/lib/modules/${OUT}

%.o: %.c
	gcc -ggdb -I${ROOT}/src/include -c -fpic $<
all: ${OBJS}
	gcc -ggdb -shared -o ${OUT} *.o
syms:
	@objdump -e ${OUT} -TS
